# 本地开发：覆盖源码与前端，改代码后重启容器即可（需与 docker-compose.yml 一起使用）
# 用法：docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d
services:
  reggraph-assistant:
    environment:
      PYTHONPATH: /app/src
